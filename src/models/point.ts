/**
 * Represents a 2D point in a Cartesian coordinate system.
 * Used for geometric calculations within the room.
 */
export class Point {
  constructor(
    public x: number,
    public y: number
  ) {}
}
